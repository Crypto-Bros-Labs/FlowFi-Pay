export interface TokenData {
    tokenNetworkUuid: string;
    symbol: string;
    network: string;
    address: string;
    imageUrl: string;
}

export interface ProviderData {
    uuid: string;
    name: string;
}